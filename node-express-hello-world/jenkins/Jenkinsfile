pipeline {
    agent {
        docker { 
            image 'node:8' 
        }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
        stage('Npm Install') {
            steps {
                dir('node-express-hello-world') { 
                    sh 'npm cache clean --force'
                    sh 'npm install'
                }
            }
        }
        stage('Npm Start') {
            steps {
                dir('node-express-hello-world') { 
                    sh 'npm start' 
                }
            }
        }
    }
}
