pipeline {
    agent {
        docker {
            image 'maven:3.9.0'
            args '-v /root/.m2:/root/.m2'
        }
    }
    stages {
        stage('Build') {
            steps {
                dir('simple-java-maven-app') {
                        // Run your commands or perform operations in the specified directory
                        sh 'ls'  // Example shell command
                        sh 'echo "Hello, World!"'  // Another example shell command
                    'mvn -B -DskipTests clean package'
                    }
        
            }
        }
        stage('Test') {
            steps {
                sh 'mvn test'
            }
            post {
                always {
                    junit 'target/surefire-reports/*.xml'
                }
            }
        }
        stage('Deliver') {
            steps {
                sh './jenkins/scripts/deliver.sh'
            }
        }
    }
}
